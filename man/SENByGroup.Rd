% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senByGroup.R
\name{SENByGroup}
\alias{SENByGroup}
\title{Summarize Extended Network (SEN) Outcomes by Kinship Group}
\usage{
SENByGroup(
  df_foldername = "longevity_skinny_matpat",
  binwidth_num = c(0.1, 0.05),
  binwidth_cha = c("10", "05"),
  kin_degree_max = 12,
  kin_degree_min = 0,
  max_kin_per_bin = 8.7 * 10^9,
  cnu = c(1, 0),
  mit = c(1, 0),
  doubleentered = TRUE,
  slice_1000 = TRUE,
  cleanup = TRUE,
  drop_variables = c("mitRel", "USA_quantile_k1", "USA_quantile_k2",
    "SWE_quantile_k1", "SWE_quantile_k2"),
  outcome_vars = c("USA_flag_10", "USA_flag_10"),
  outcome_functions = c("polychorFunction", "ml_polychorFunction"),
  grouping_vars = NA,
  grouping_filename = NULL,
  verbose = TRUE,
  mutate_vars = NULL,
  memory_manage = 0L,
  data_path = "",
  file_path_stem = "U:/IRB_00143000/mtdna/aim1_cor/cor_",
  skinny_summarize_call = TRUE,
  age_filter = FALSE,
  min_age = 0
)
}
\arguments{
\item{df_foldername}{Character string.  Name of the data folder/dataset.
Default is \code{"longevity_skinny_matpat"}.}

\item{binwidth_num}{Numeric vector of bin half-widths.  Must be the same
length as \code{binwidth_cha}.  Default is \code{c(0.1, 0.05)}.}

\item{binwidth_cha}{Character vector of bin-width labels used in file
names.  Default is \code{c("10", "05")}.}

\item{kin_degree_max}{Integer.  Maximum kinship degree.  Default is
\code{12}.}

\item{kin_degree_min}{Integer.  Minimum kinship degree.  Default is
\code{0}.}

\item{max_kin_per_bin}{Numeric.  Bins exceeding this row count are skipped.
Default is \code{8.7e9}.}

\item{cnu}{Integer vector of CNU values to loop over.  Default is
\code{c(1, 0)}.}

\item{mit}{Integer vector of mtDNA relatedness values to loop over.
Default is \code{c(1, 0)}.}

\item{doubleentered}{Logical.  If \code{TRUE} (default), each dyadic data
set is double-entered.}

\item{slice_1000}{Logical.  If \code{TRUE} (default), only the first 1 000
rows of each bin are processed.}

\item{cleanup}{Logical.  If \code{TRUE} (default), temporary scratch files
are removed on exit.}

\item{drop_variables}{Character vector of column names to drop when reading
bin files.}

\item{outcome_vars}{Character vector of outcome variable base names.
Default is \code{c("USA_flag_10", "USA_flag_10")}.}

\item{outcome_functions}{Character vector of function names to apply to
each outcome pair.  Default is
\code{c("polychorFunction", "ml_polychorFunction")}.}

\item{grouping_vars}{Character vector of additional grouping variables, or
\code{NA} for none.  Default is \code{NA}.}

\item{grouping_filename}{Character string for the output file name, or
\code{NULL}.  Default is \code{NULL}.}

\item{verbose}{Logical.  If \code{TRUE} (default), progress messages are
emitted.}

\item{mutate_vars}{Character string (or \code{NULL}) passed to
\code{\link{mutateFunction}}.  Default is \code{NULL}.}

\item{memory_manage}{Integer memory-management flag (\code{0L}, \code{1L},
or \code{2L}).  Default is \code{0L}.}

\item{data_path}{Character string giving the root data directory.  Default
is \code{""}.}

\item{file_path_stem}{Character string giving the output CSV path stem.
Default is \code{"U:/IRB_00143000/mtdna/aim1_cor/cor_"}.}

\item{skinny_summarize_call}{Logical.  If \code{TRUE} (default), empirical
\code{addRel} distribution statistics are omitted from the summary.}

\item{age_filter}{Logical.  If \code{TRUE}, kin younger than
\code{min_age} are excluded.  Default is \code{FALSE}.}

\item{min_age}{Numeric.  Minimum age when \code{age_filter = TRUE}.
Default is \code{0}.}
}
\value{
Called primarily for its side effect of writing a CSV file.
Returns \code{NULL} invisibly.
}
\description{
A variant of \code{\link{correlateOutcomesByGroup}} that additionally
computes extended network statistics (unique individual counts, average
dyads per ID, etc.) for each relatedness bin.  The function loops over
bins of additive-genetic relatedness, mtDNA relatedness, and CNU status;
reads pre-built bin files; double-enters the data (optional); and
summarizes the requested outcomes.  Results are appended to a CSV file.
}
